services:
  - type: web
    name: bharattrust-api
    env: python
    rootDir: backend
    buildCommand: "pip install -r requirements.txt && python -m app.seed"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: FRONTEND_ORIGIN
        value: "*"
