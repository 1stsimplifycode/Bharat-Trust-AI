# Root requirements — what Vercel installs for the serverless function.
# `./backend` installs the application itself (the `app` package) into
# site-packages, which is the only location serverless bundlers ship
# unconditionally. Deliberately no scikit-learn/scipy here: the fraud agent
# auto-falls back to its pure-Python detector to keep the bundle small.
# Local/Render runs use backend/requirements.txt with the full ML stack.
fastapi==0.115.0
uvicorn==0.30.6
sqlalchemy==2.0.34
pydantic==2.9.2
python-dotenv==1.0.1
./backend
